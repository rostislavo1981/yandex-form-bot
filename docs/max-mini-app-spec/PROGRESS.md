# PROGRESS

Обновлено: 2026-07-14.

## Текущий статус

Документация MVP согласована. Код нового проекта ещё не начат.

| Итерация | Статус | Результат |
|---|---|---|
| I00 | NEXT | Скелет `max_daily_report/`, health, PostgreSQL compose, тест |
| I01–I22 | WAIT | Выполняются строго по порядку |

## Текущая итерация: I00

Следующий агент делает только I00 из `08_implementation_plan.md`.

Перед началом нужно одно подтверждение пользователя, только если расположение изменилось: по умолчанию новый код создаётся в `max_daily_report/` внутри текущего репозитория.

## Чек-лист I00

- [ ] Создана подпапка `max_daily_report/`.
- [ ] Настроен Python package и зависимости.
- [ ] Есть FastAPI `GET /api/health`.
- [ ] Есть Settings и `.env.example` без секретов.
- [ ] Docker Compose поднимает PostgreSQL.
- [ ] Есть Dockerfile и Makefile.
- [ ] Есть smoke test health.
- [ ] `pytest` и `ruff check` зелёные.
- [ ] Добавлен handoff и один коммит.

## HANDOFF NOTES

### 2026-07-14 — спецификация переписана под окончательный MVP

**Фаза:** documentation reset перед I00.

**Сделано:** выбран Mini App-first; добавлены динамические каталоги/Excel, obligations, групповые и личные кнопки, напоминания, morning summary, табель отдельно по объектам; план разбит на I00–I22; исправлена спецификация MAX initData.

**Код:** не создавался.

**Следующий шаг:** выполнить только I00.
